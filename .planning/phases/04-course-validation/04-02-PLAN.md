---
phase: 04-course-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [course/day1.ipynb]
autonomous: true
gap_closure: true
requirements: [COURSE-10, COURSE-11, COURSE-12]

must_haves:
  truths:
    - "User can see actual execution outputs from DataTalks.Club FAQ repository test"
    - "User can see actual execution outputs from Evidently AI docs repository test"
    - "Notebook file contains committed outputs proving both tests executed successfully"
  artifacts:
    - path: "course/day1.ipynb"
      provides: "Notebook with committed execution outputs from both repository tests"
      contains: "Successfully processed"
  key_links:
    - from: "course/day1.ipynb cell 11"
      to: "GitHub API (DataTalksClub/faq)"
      via: "read_repo_data() function call with network request"
      pattern: "datatalk_docs = read_repo_data"
    - from: "course/day1.ipynb cell 13"
      to: "GitHub API (evidentlyai/docs)"
      via: "read_repo_data() function call with network request"
      pattern: "evidently_docs = read_repo_data"
---

<objective>
Execute the course/day1.ipynb notebook from a fresh kernel and commit the outputs to provide behavioral verification that both repository tests (DataTalks.Club FAQ and Evidently AI docs) run successfully and return expected data structures.

Purpose: Close verification gap by providing runtime evidence that the implementation works, not just structural evidence that the code exists.

Output: Updated course/day1.ipynb with committed execution outputs showing successful downloads and parsed data from both test repositories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-course-validation/04-VERIFICATION.md

## Gap Analysis

From VERIFICATION.md:

**Gap 1:** Truth "User can execute notebook cell that downloads DataTalks.Club FAQ repository and see parsed output"
- Status: PARTIAL
- Reason: Test cell code exists but no execution outputs committed
- Missing: Execute notebook from fresh kernel and commit outputs

**Gap 2:** Truth "User can execute notebook cell that downloads Evidently AI docs repository and see parsed output"
- Status: PARTIAL
- Reason: Test cell code exists but no execution outputs committed
- Missing: Execute notebook from fresh kernel and commit outputs

**Gap 3:** Truth "Full notebook executes successfully from fresh kernel with all test outputs visible"
- Status: FAILED
- Reason: Notebook file contains no execution outputs (all 8 code cells show outputs=[])
- Missing: Run jupyter nbconvert --execute or manually execute all cells

**Root Cause:** The notebook was structured and committed, but not executed from a fresh kernel with outputs saved. This is purely an execution gap, not an implementation gap.

## Expected Outputs

From Phase 4 Plan 01 SUMMARY.md test results:

**DataTalks.Club FAQ:**
- 24 documents processed
- Markdown (.md) files
- YAML frontmatter with title, description, tags

**Evidently AI docs:**
- 141 documents processed
- Markdown (.md) and MDX (.mdx) files
- Complex YAML frontmatter

Expected visible output pattern in each test cell:
```
================================================================================
TEST 1: DataTalks.Club FAQ Repository
================================================================================
Downloading DataTalksClub/faq from https://codeload.github.com/...
Downloaded N bytes
Total files in archive: N
Markdown files found: N
Successfully processed 24 documents

✓ Successfully processed 24 documents

First 3 documents:

1. {filename}
   Metadata keys: [...]
   Content length: N characters
   Sample metadata: {...}
```
</context>

<tasks>

<task type="auto">
  <name>Task 0: Execute notebook from fresh kernel and commit outputs</name>

  <read_first>
    - course/day1.ipynb (to verify current state before execution)
  </read_first>

  <files>course/day1.ipynb</files>

  <action>
Execute the complete course/day1.ipynb notebook from a fresh kernel using jupyter nbconvert with proper configuration:

1. Navigate to the course/ directory (notebook expects to run from there)

2. Execute the notebook with these parameters:
   - Kernel: python3 (the course uv environment)
   - Timeout: 300 seconds (5 minutes, sufficient for two GitHub downloads)
   - Execution mode: in-place (update the original file with outputs)
   - Store execution metadata

Command to use:
```bash
cd course && \
jupyter nbconvert \
  --to notebook \
  --execute \
  --inplace \
  --ExecutePreprocessor.kernel_name=python3 \
  --ExecutePreprocessor.timeout=300 \
  day1.ipynb
```

3. After execution, verify outputs are present:
   - All code cells should have non-empty outputs[] arrays
   - Cell 11 (DataTalks test) should show "Successfully processed 24 documents"
   - Cell 13 (Evidently test) should show "Successfully processed 141 documents"
   - No execution errors in any cell

4. Commit the updated notebook with outputs using GPG signing (required by CLAUDE.md lines 65-71):
```bash
git add course/day1.ipynb && git commit -S -m "feat(04-02): execute notebook from fresh kernel and commit outputs"
```

**Why this closes the gap:**
- Transforms structural verification (code exists) into behavioral verification (code runs)
- Provides visible evidence in the notebook file that both repository tests executed successfully
- Satisfies COURSE-10, COURSE-11, COURSE-12 requirements fully (not just partially)
- Allows human reviewers to see actual outputs without re-running the notebook

**Network dependency:** This task requires internet access to download from GitHub's codeload API. If downloads fail due to network issues, the task cannot complete.
  </action>

  <verify>
    <automated>
grep -q '"outputs":\s*\[\s*{' course/day1.ipynb && \
grep -q 'Successfully processed 24 documents' course/day1.ipynb && \
grep -q 'Successfully processed 141 documents' course/day1.ipynb
    </automated>
  </verify>

  <acceptance_criteria>
    - course/day1.ipynb file contains non-empty outputs[] arrays in code cells
    - Cell 11 output contains text "Successfully processed 24 documents"
    - Cell 13 output contains text "Successfully processed 141 documents"
    - Cell 11 output shows "TEST 1: DataTalks.Club FAQ Repository" header
    - Cell 13 output shows "TEST 2: Evidently AI Documentation" header
    - Cell 11 output displays first 3 document details (filenames, metadata keys, content lengths)
    - Cell 13 output displays first 3 document details (filenames, metadata keys, content lengths)
    - No execution errors visible in any cell outputs
    - Notebook JSON shows execution_count values incremented for all code cells
    - Git commit exists with GPG signature and message containing "execute notebook" and "commit outputs"
  </acceptance_criteria>

  <done>
course/day1.ipynb contains committed execution outputs from fresh kernel run, showing successful downloads and processing of 24 documents from DataTalks.Club FAQ and 141 documents from Evidently AI docs, with visible output formatting displaying document counts, filenames, metadata keys, and content previews for both repositories. All three course validation requirements (COURSE-10, COURSE-11, COURSE-12) are now fully satisfied with behavioral verification, not just structural verification.
  </done>
</task>

</tasks>

<verification>
**Overall Phase Verification:**

1. Verify notebook has execution outputs:
```bash
# Check that outputs arrays are no longer empty
python3 -c "import json; nb=json.load(open('course/day1.ipynb')); \
  cells_with_outputs = sum(1 for c in nb['cells'] if c['cell_type']=='code' and len(c.get('outputs', []))>0); \
  print(f'Code cells with outputs: {cells_with_outputs}'); \
  exit(0 if cells_with_outputs >= 8 else 1)"
```

2. Verify DataTalks test output visible:
```bash
grep -A 20 '"TEST 1: DataTalks.Club FAQ Repository"' course/day1.ipynb | \
  grep -q 'Successfully processed 24 documents'
```

3. Verify Evidently test output visible:
```bash
grep -A 20 '"TEST 2: Evidently AI Documentation"' course/day1.ipynb | \
  grep -q 'Successfully processed 141 documents'
```

4. Verify no execution errors:
```bash
# Check for error outputs in notebook JSON
python3 -c "import json; nb=json.load(open('course/day1.ipynb')); \
  errors = [c for c in nb['cells'] if c['cell_type']=='code' \
    for o in c.get('outputs', []) if o.get('output_type')=='error']; \
  print(f'Cells with errors: {len(errors)}'); \
  exit(1 if errors else 0)"
```

5. Confirm requirements met:
   - COURSE-10: DataTalks.Club FAQ test executed with visible outputs ✓
   - COURSE-11: Evidently AI docs test executed with visible outputs ✓
   - COURSE-12: Sample outputs displaying parsed data structure ✓
</verification>

<success_criteria>
**Gap closure complete when:**

1. course/day1.ipynb has been executed from fresh kernel using jupyter nbconvert
2. All code cells in notebook contain execution outputs (outputs[] arrays are non-empty)
3. Cell 11 output shows "Successfully processed 24 documents" from DataTalks.Club FAQ test
4. Cell 13 output shows "Successfully processed 141 documents" from Evidently AI docs test
5. Both test outputs display first 3 document details showing filenames, metadata keys, content lengths
6. No execution errors present in any cell outputs
7. Notebook file committed to git with GPG-signed commit and execution outputs included
8. Re-verification of 04-VERIFICATION.md would now show all 6 truths as VERIFIED (not PARTIAL/FAILED)
9. All three phase requirements (COURSE-10, COURSE-11, COURSE-12) satisfied with behavioral evidence

**Gap closure metric:** All notebook cells executed, outputs visible, zero PARTIAL/FAILED truths remaining.
</success_criteria>

<output>
After completion, create `.planning/phases/04-course-validation/04-02-SUMMARY.md` documenting:
- Execution results from fresh kernel run
- Actual document counts from both repositories (should match 24 and 141)
- Confirmation that outputs are now committed and visible in notebook
- Any execution warnings or issues encountered (network timeouts, etc.)
- Verification that gap is fully closed (behavioral verification achieved)
- Updated status of COURSE-10, COURSE-11, COURSE-12 requirements (all COMPLETE with evidence)
</output>
