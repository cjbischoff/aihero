---
phase: 01-environment-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - course/
  - project/
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-03
  - ENV-04

must_haves:
  truths:
    - "Python 3.10+ is verified and accessible via command line"
    - "uv package manager is verified and responds to version command"
    - "Folder structure exists with course/ and project/ subdirectories"
    - "Developer can initialize uv projects in both folders without errors"
  artifacts:
    - path: "course/"
      provides: "Container for course reproduction work"
      min_lines: 0
    - path: "project/"
      provides: "Container for personal homework implementations"
      min_lines: 0
  key_links:
    - from: "Python executable"
      to: "uv package manager"
      via: "uv uses system Python"
      pattern: "python.*3\\.1[0-9]"
---

<objective>
Verify developer environment readiness and create project folder structure.

Purpose: Ensure Python 3.10+, uv package manager, and workspace folders are ready before any development work begins.

Output: Verified Python/uv installation and created course/ and project/ directories with verified uv initialization capability.
</objective>

<execution_context>
@/Users/christopher/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christopher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Python and uv installations</name>
  <files>N/A - verification only</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 12-15 for ENV-01 and ENV-02 requirements)
  </read_first>
  <action>
Verify Python 3.10+ is installed and working:
- Run `python3 --version` and confirm output shows Python 3.10 or higher
- Verify Python is accessible in PATH

Verify uv package manager is installed and working:
- Run `uv --version` and confirm command responds without error
- Verify uv is accessible in PATH

Both tools must respond successfully for environment to be considered ready.
  </action>
  <verify>
    <automated>python3 --version && uv --version</automated>
  </verify>
  <acceptance_criteria>
    - `python3 --version` exits with code 0 and output contains "Python 3.1" or "Python 3.2" (3.10+)
    - `uv --version` exits with code 0 and displays version number
  </acceptance_criteria>
  <done>Python 3.10+ verified and uv package manager verified and accessible via command line</done>
</task>

<task type="auto">
  <name>Task 2: Create folder structure</name>
  <files>course/, project/</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 15-16 for ENV-03 and ENV-04 requirements)
    - .planning/PROJECT.md (lines 62-72 for folder structure context)
  </read_first>
  <action>
Create two subdirectories in the project root:

1. Create `course/` directory:
   - Purpose: Container for course reproduction work (day1.ipynb, day2.ipynb, etc.)
   - No initial files needed - will be populated in Phase 2

2. Create `project/` directory:
   - Purpose: Container for personal homework implementations (different from course reproductions)
   - No initial files needed - will be populated in Phase 5

Both directories should be empty at creation time. They serve as organizational boundaries between course reproductions and personal implementations.
  </action>
  <verify>
    <automated>ls -d course/ && ls -d project/</automated>
  </verify>
  <acceptance_criteria>
    - `ls -d course/` exits with code 0 and displays "course/"
    - `ls -d project/` exits with code 0 and displays "project/"
    - Both directories exist and are empty (no files inside)
  </acceptance_criteria>
  <done>Directories course/ and project/ exist in project root</done>
</task>

<task type="auto">
  <name>Task 3: Verify uv project initialization capability</name>
  <files>N/A - verification only, cleanup after test</files>
  <read_first>
    - .planning/ROADMAP.md (lines 28-33 for Phase 1 success criteria #4)
  </read_first>
  <action>
Verify uv can successfully initialize projects in both folders:

1. Test uv initialization in course/ folder:
   - Navigate to course/ directory
   - Run `uv init --no-readme --name course-test` to create a test project
   - Verify pyproject.toml is created
   - Clean up test: remove pyproject.toml (Phase 2 will create proper project)

2. Test uv initialization in project/ folder:
   - Navigate to project/ directory
   - Run `uv init --no-readme --name project-test` to create a test project
   - Verify pyproject.toml is created
   - Clean up test: remove pyproject.toml (Phase 5 will create proper project)

This verification confirms uv is working correctly and can manage projects in both workspaces before actual development begins.
  </action>
  <verify>
    <automated>cd course && uv init --no-readme --name course-test && test -f pyproject.toml && rm pyproject.toml && cd ../project && uv init --no-readme --name project-test && test -f pyproject.toml && rm pyproject.toml && cd ..</automated>
  </verify>
  <acceptance_criteria>
    - `uv init` command succeeds in both course/ and project/ directories
    - pyproject.toml files are created in both locations during test
    - Both directories are empty after cleanup (no pyproject.toml remains)
    - Command exits with code 0
  </acceptance_criteria>
  <done>Verified uv can initialize projects in both course/ and project/ folders without errors, cleanup completed</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:
1. `python3 --version` shows Python 3.10 or higher
2. `uv --version` shows uv is installed
3. `ls -d course/ project/` shows both directories exist
4. Both directories are empty and ready for Phase 2 and Phase 5 work
</verification>

<success_criteria>
- Python 3.10+ is verified and accessible
- uv package manager is verified and functional
- course/ directory exists and is empty
- project/ directory exists and is empty
- uv can successfully initialize projects in both folders
- All requirements ENV-01, ENV-02, ENV-03, ENV-04 are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-environment-setup/01-01-SUMMARY.md`
</output>
