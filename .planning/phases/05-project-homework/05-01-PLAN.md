---
phase: 05-project-homework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - project/pyproject.toml
  - project/.python-version
  - project/.pre-commit-config.yaml
  - project/owasp_homework.ipynb
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04
  - PROJ-05
  - PROJ-06
  - PROJ-07
  - PROJ-08

must_haves:
  truths:
    - "User can see project/ folder has valid uv project configuration"
    - "User can install dependencies in project/ environment without errors"
    - "User can execute notebook from fresh kernel and see OWASP repository downloaded"
    - "User can see parsed documents with metadata and content structure"
    - "User can observe differences between OWASP repo (no frontmatter) and course repos (with frontmatter)"
  artifacts:
    - path: "project/pyproject.toml"
      provides: "uv project configuration with dependencies"
      contains: "requests"
    - path: "project/.python-version"
      provides: "Python version specification"
      contains: "3.13"
    - path: "project/.pre-commit-config.yaml"
      provides: "Pre-commit hook configuration"
      contains: "ruff"
    - path: "project/owasp_homework.ipynb"
      provides: "Working notebook with OWASP implementation"
      min_lines: 100
  key_links:
    - from: "project/owasp_homework.ipynb"
      to: "read_repo_data() function"
      via: "function definition in notebook cell"
      pattern: "def read_repo_data"
    - from: "project/owasp_homework.ipynb"
      to: "OWASP repository"
      via: "GitHub codeload API call"
      pattern: "OWASP.*www-project-top-10"
---

<objective>
Apply the learned `read_repo_data()` pattern from course implementation to a new repository (OWASP LLM Top 10) in a separate project/ folder. This demonstrates ability to adapt course patterns to different repository structures and validates understanding through educational commentary.

Purpose: Demonstrate mastery of Day 1 concepts by successfully adapting them to a different repository structure (OWASP has no frontmatter, different organization). This is the homework component that proves understanding beyond course reproduction.

Output: Working project/ folder with uv configuration, pre-commit hooks, executable Jupyter notebook showing successful OWASP repository processing, and educational commentary explaining structural differences and adaptation approach.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-project-homework/05-CONTEXT.md
@.planning/phases/05-project-homework/05-RESEARCH.md

# Reference implementation from course
@course/day1.ipynb
@course/pyproject.toml

# Project engineering standards
@CLAUDE.md
</context>

<interfaces>
<!-- Key patterns from course implementation that executor will adapt -->

From course/day1.ipynb - Helper Functions:
```python
def download_repo_zip(repo_owner: str, repo_name: str) -> bytes:
    """Download GitHub repository as zip archive via codeload API."""
    # Returns binary zip content

def is_markdown_file(filename: str) -> bool:
    """Check if file is markdown (.md or .mdx)."""
    # Returns True/False

def parse_markdown_with_frontmatter(content_bytes: bytes) -> Dict[str, Any]:
    """Parse markdown extracting frontmatter and content."""
    # Returns {'metadata': dict, 'content': str}

def read_repo_data(repo_owner: str, repo_name: str) -> List[Dict[str, Any]]:
    """Main orchestration function."""
    # Returns list of docs, each with filename, metadata, content
```

From course/pyproject.toml - Dependencies:
```toml
dependencies = ["requests>=2.32.0", "python-frontmatter>=1.1.0"]
[project.optional-dependencies]
dev = ["jupyter>=1.0.0"]
```

Document Structure Pattern:
```python
{
    'filename': 'path/to/file.md',
    'metadata': {},  # Empty if no frontmatter (OWASP case)
    'content': 'markdown content string'
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project/ uv environment with pre-commit hooks</name>
  <read_first>
    - course/pyproject.toml (dependency specifications to mirror)
    - .planning/phases/05-project-homework/05-CONTEXT.md (D-01, D-02: project structure decisions)
    - .planning/phases/05-project-homework/05-RESEARCH.md (Standard Stack section: exact versions)
    - CLAUDE.md (Linting and Formatting section: pre-commit requirements)
  </read_first>
  <files>
    project/pyproject.toml
    project/.python-version
    project/.gitignore
    project/.pre-commit-config.yaml
  </files>
  <action>
Initialize uv project in project/ folder per D-01 with same dependencies as course/ per D-02, and set up pre-commit hooks per CLAUDE.md §Linting and Formatting requirements.

**Step 1: Initialize uv project**

Commands to execute:
```bash
cd project/
uv init --name aihero-project --app
uv add requests>=2.32.0 python-frontmatter>=1.1.0
uv add --dev jupyter>=1.0.0 pre-commit ruff black
```

This creates:
- pyproject.toml with project metadata and dependencies
- .python-version pinned to 3.13 (matching course/)
- .venv/ virtual environment (auto-created by uv)
- main.py placeholder (will be unused, notebook is the artifact)

**Step 2: Add tool configuration to pyproject.toml**

Add the following sections to pyproject.toml for ruff and black configuration (simplified for learning project, not full CLAUDE.md config):

```toml
[tool.ruff]
line-length = 100
target-version = "py313"
select = ["E", "F", "W", "I"]

[tool.black]
line-length = 100
target-version = ["py313"]
```

**Step 3: Create .pre-commit-config.yaml**

Create minimal pre-commit configuration appropriate for a learning exercise (basic formatting/linting only, not full security suite):

```yaml
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files

  - repo: https://github.com/psf/black
    rev: 24.3.0
    hooks:
      - id: black

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.3.0
    hooks:
      - id: ruff
        args: [--fix]
```

This configuration includes:
- Basic file hygiene (trailing whitespace, end-of-file)
- Black formatting (auto-format)
- Ruff linting with auto-fix
- Excludes heavy security tools (Snyk, Semgrep) appropriate for learning project
- Per CLAUDE.md §Notebooks: project/ folder applies engineering standards, unlike course/

**Step 4: Install pre-commit hooks**

```bash
cd project/
uv run pre-commit install
```

This installs the Git hooks so they run automatically on every commit.

**Step 5: Add .gitignore**

Add .gitignore with standard Python exclusions:
```
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.pytest_cache/
.coverage
*.egg-info/
```

The project structure mirrors course/ setup but follows engineering standards per D-03 (type hints, docstrings, proper formatting, pre-commit enforcement).
  </action>
  <acceptance_criteria>
    - `test -f project/pyproject.toml` succeeds
    - `test -f project/.python-version` succeeds and contains "3.13"
    - `test -f project/.pre-commit-config.yaml` succeeds
    - `grep -q "requests" project/pyproject.toml` succeeds
    - `grep -q "python-frontmatter" project/pyproject.toml` succeeds
    - `grep -q "jupyter" project/pyproject.toml` succeeds
    - `grep -q "pre-commit" project/pyproject.toml` succeeds
    - `grep -q "ruff" project/pyproject.toml` succeeds
    - `grep -q "black" project/pyproject.toml` succeeds
    - `test -f project/.gitignore` succeeds
    - `test -f project/.git/hooks/pre-commit` succeeds (hooks installed)
    - `uv pip list | grep -E "(requests|frontmatter|jupyter|pre-commit|ruff|black)"` from project/ shows all dependencies installed
  </acceptance_criteria>
  <verify>
    <automated>cd project/ && test -f .pre-commit-config.yaml && test -f .git/hooks/pre-commit && uv pip list | grep -E "(requests|frontmatter|jupyter|pre-commit|ruff|black)"</automated>
  </verify>
  <done>
project/ folder has valid uv project with pyproject.toml declaring requests, python-frontmatter, jupyter, pre-commit, ruff, and black dependencies. .python-version pins to 3.13. .pre-commit-config.yaml defines minimal hooks (black, ruff, basic file hygiene). Pre-commit hooks are installed in .git/hooks/. .gitignore excludes generated files. Dependencies are installed in .venv/.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create notebook with OWASP implementation</name>
  <read_first>
    - course/day1.ipynb (reference implementation to adapt)
    - .planning/phases/05-project-homework/05-CONTEXT.md (D-04 through D-13: implementation decisions)
    - .planning/phases/05-project-homework/05-RESEARCH.md (Common Pitfalls section: no frontmatter handling, display patterns)
  </read_first>
  <files>
    project/owasp_homework.ipynb
  </files>
  <behavior>
    - Notebook imports required libraries (requests, zipfile, BytesIO, frontmatter, pathlib, typing)
    - Helper functions defined: download_repo_zip, is_markdown_file, parse_markdown_with_frontmatter (copied from course per D-04, D-05)
    - Main function read_repo_data defined with full type hints and Google-style docstring (engineering standard per D-03)
    - Test cell downloads OWASP repo: read_repo_data("OWASP", "www-project-top-10-for-large-language-model-applications") per D-06
    - Display cell shows: total count, frontmatter vs no-frontmatter breakdown, sample documents (first 3) per D-10, D-11
    - Handles empty metadata gracefully (OWASP has no frontmatter per D-07, Research §Common Pitfalls §1)
    - Educational cells explain: OWASP structure differences, how same code handles different repos, comparison with course repos per D-09, D-13
  </behavior>
  <action>
Create Jupyter notebook at project/owasp_homework.ipynb following educational structure per Research §Architecture Patterns §Pattern 1.

**Cell structure:**

**Cell 1 (markdown):**
```markdown
# Project Homework: Applying GitHub Ingestion to OWASP LLM Top 10

This notebook applies the `read_repo_data()` pattern learned in the course to a different repository structure. The OWASP LLM Top 10 repository provides security documentation for large language model applications.

## What's Different

Unlike the course repositories (DataTalks FAQ and Evidently docs):
- **OWASP has NO frontmatter** - files are plain markdown without YAML metadata blocks
- **Different organization** - nested structure with 2_0_vulns/, documentation/, translations/ folders
- **More files** - 542 markdown documents vs 95-1285 in course repos

## Key Learning

The same `read_repo_data()` implementation works across all repositories because:
1. python-frontmatter gracefully handles files without frontmatter (returns empty metadata dict)
2. In-memory zip processing works regardless of repository structure
3. Markdown filtering works uniformly across file organizations
```

**Cell 2 (code):**
```python
# Import required libraries
import requests
from zipfile import ZipFile
from io import BytesIO
import frontmatter
from pathlib import Path
from typing import List, Dict, Any
```

**Cell 3 (markdown):**
```markdown
## Helper Functions

These functions are copied from course/day1.ipynb (per learning objective - adaptation through reuse, not reinvention). Each function has been enhanced with type hints and docstrings per project/ engineering standards.
```

**Cell 4 (code):**
```python
def download_repo_zip(repo_owner: str, repo_name: str) -> bytes:
    """Download GitHub repository as zip archive.

    Uses GitHub's codeload API which provides repositories as zip archives
    without requiring authentication for public repos.

    Args:
        repo_owner: GitHub username or organization (e.g., 'OWASP')
        repo_name: Repository name (e.g., 'www-project-top-10-for-large-language-model-applications')

    Returns:
        Binary content of zip archive

    Raises:
        requests.HTTPError: If download fails (repo not found, network error, etc.)
    """
    url = f"https://codeload.github.com/{repo_owner}/{repo_name}/zip/refs/heads/main"
    print(f"Downloading {repo_owner}/{repo_name} from {url}")

    response = requests.get(url)
    response.raise_for_status()

    print(f"Downloaded {len(response.content)} bytes")
    return response.content
```

**Cell 5 (code):**
```python
def is_markdown_file(filename: str) -> bool:
    """Check if file is a markdown document (.md or .mdx).

    Args:
        filename: Full path to file in zip archive

    Returns:
        True if file ends with .md or .mdx, False otherwise
    """
    path = Path(filename)
    return path.suffix in ['.md', '.mdx']
```

**Cell 6 (code):**
```python
def parse_markdown_with_frontmatter(content_bytes: bytes) -> Dict[str, Any]:
    """Parse markdown file extracting frontmatter metadata and content.

    Handles files without frontmatter gracefully - returns empty metadata dict.
    This is critical for OWASP repository which has no frontmatter.

    Args:
        content_bytes: Raw file content as bytes

    Returns:
        Dictionary with:
        - 'metadata': dict of frontmatter key-value pairs (empty {} if no frontmatter)
        - 'content': str of markdown content after frontmatter (or full content if none)
    """
    content_str = content_bytes.decode('utf-8', errors='ignore')
    post = frontmatter.loads(content_str)

    return {
        'metadata': dict(post.metadata),  # Empty dict {} if no frontmatter
        'content': post.content
    }
```

**Cell 7 (markdown):**
```markdown
## Main Function: read_repo_data()

This is the same orchestration logic from the course, proving that good abstractions work across different repository structures.
```

**Cell 8 (code):**
```python
def read_repo_data(repo_owner: str, repo_name: str) -> List[Dict[str, Any]]:
    """Download GitHub repository and extract markdown documentation with metadata.

    This implementation is identical to course/day1.ipynb because the core
    pattern is universal. The interesting part is how it handles repositories
    with different structures and frontmatter conventions.

    Args:
        repo_owner: GitHub username or organization (e.g., 'OWASP')
        repo_name: Repository name

    Returns:
        List of dictionaries, each containing:
        - 'filename': str - path to file within repository
        - 'metadata': dict - YAML frontmatter key-value pairs (empty {} if no frontmatter)
        - 'content': str - markdown content after frontmatter (or full content if none)
    """
    # Download repository as zip
    zip_content = download_repo_zip(repo_owner, repo_name)

    # Open zip archive in memory
    zip_file = ZipFile(BytesIO(zip_content))

    # Get all files
    all_files = zip_file.namelist()
    print(f"Total files in archive: {len(all_files)}")

    # Filter for markdown files
    markdown_files = [f for f in all_files if is_markdown_file(f)]
    print(f"Markdown files found: {len(markdown_files)}")

    # Process each markdown file
    documents = []
    for filename in markdown_files:
        file_content = zip_file.read(filename)
        parsed = parse_markdown_with_frontmatter(file_content)

        doc = {
            'filename': filename,
            'metadata': parsed['metadata'],
            'content': parsed['content']
        }
        documents.append(doc)

    print(f"Successfully processed {len(documents)} documents")
    return documents
```

**Cell 9 (markdown):**
```markdown
## Testing with OWASP LLM Top 10 Repository

Repository: https://github.com/OWASP/www-project-top-10-for-large-language-model-applications

This repository documents the top 10 security risks for LLM applications. It has a different structure than our course examples and uses plain markdown (no frontmatter).
```

**Cell 10 (code):**
```python
# Download and process OWASP repository
docs = read_repo_data("OWASP", "www-project-top-10-for-large-language-model-applications")
```

**Cell 11 (code):**
```python
# Display repository characteristics
print(f"OWASP LLM Top 10 Repository Analysis")
print(f"=" * 80)
print(f"Total documents: {len(docs)}")
print(f"Documents with frontmatter: {sum(1 for d in docs if d['metadata'])}")
print(f"Documents without frontmatter: {sum(1 for d in docs if not d['metadata'])}")
print()

# Show sample documents
print(f"Sample documents (first 3):")
for i, doc in enumerate(docs[:3], 1):
    print(f"\n{i}. {doc['filename']}")

    # Handle empty metadata gracefully
    if doc['metadata']:
        print(f"   Metadata: {doc['metadata']}")
    else:
        print(f"   Metadata: None (no frontmatter)")

    # Show content preview
    content_preview = doc['content'][:150].replace('\n', ' ')
    print(f"   Content preview: {content_preview}...")
    print(f"   Total length: {len(doc['content'])} characters")

# Compare to course repos
print(f"\n" + "=" * 80)
print(f"Comparison with Course Repositories:")
print(f"  DataTalks FAQ:     1,285 files, most with frontmatter")
print(f"  Evidently docs:       95 files, most with frontmatter")
print(f"  OWASP LLM Top 10:   {len(docs)} files, NO frontmatter")
print(f"\nKey insight: The same code works across all repos because python-frontmatter")
print(f"gracefully handles files without frontmatter (returns empty metadata dict).")
```

**Cell 12 (markdown):**
```markdown
## What I Learned

**Repository Structure Adaptability:**
- The OWASP repository has 542 markdown files across nested directories (2_0_vulns/, documentation/, resources/)
- Unlike course repos that use frontmatter for metadata, OWASP files are plain markdown
- The `read_repo_data()` implementation handles this gracefully - no code changes needed

**Frontmatter Handling:**
- python-frontmatter library returns empty `metadata: {}` for files without frontmatter
- This makes the implementation robust - works with or without frontmatter
- Important to check `if doc['metadata']:` before displaying metadata fields to avoid confusing output

**Why This Matters for RAG:**
- Some documentation uses frontmatter (Jekyll, Hugo, Next.js sites) for structured metadata
- Other documentation is plain markdown with all structure in headers
- A good RAG ingestion pipeline must handle both cases
- Day 2 chunking will need to adapt strategy based on whether frontmatter provides structure
```

Write this as a proper Jupyter notebook JSON structure with cell IDs, cell types, and empty outputs (will be populated in Task 3).
  </action>
  <acceptance_criteria>
    - `test -f project/owasp_homework.ipynb` succeeds
    - `grep -q "def read_repo_data" project/owasp_homework.ipynb` succeeds
    - `grep -q "OWASP" project/owasp_homework.ipynb` succeeds
    - `grep -q "www-project-top-10-for-large-language-model-applications" project/owasp_homework.ipynb` succeeds
    - `grep -q "What I Learned" project/owasp_homework.ipynb` succeeds (educational reflection present)
    - `grep -q "Type hints" project/owasp_homework.ipynb` succeeds OR notebook contains function signatures with `-> Type` annotations
    - `grep -q "Args:" project/owasp_homework.ipynb` succeeds (Google-style docstrings present)
  </acceptance_criteria>
  <verify>
    <automated>test -f project/owasp_homework.ipynb && grep -q "read_repo_data" project/owasp_homework.ipynb && grep -q "OWASP" project/owasp_homework.ipynb</automated>
  </verify>
  <done>
project/owasp_homework.ipynb exists with complete implementation copied from course, enhanced with type hints and docstrings per engineering standards. Notebook includes educational cells explaining OWASP structural differences, test cells downloading and displaying OWASP repository, and reflection on what was learned through adaptation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Execute notebook, verify pre-commit hooks, and commit outputs</name>
  <read_first>
    - project/owasp_homework.ipynb (notebook to execute)
    - .planning/phases/04-course-validation/04-02-PLAN.md (execution pattern reference)
    - .planning/phases/05-project-homework/05-CONTEXT.md (D-10, D-11, D-12: success demonstration requirements)
    - CLAUDE.md (§Linting and Formatting: pre-commit verification requirements)
  </read_first>
  <files>
    project/owasp_homework.ipynb
  </files>
  <action>
Execute the notebook from a fresh kernel to populate outputs, verify pre-commit hooks pass per CLAUDE.md §Linting and Formatting, then commit the executed notebook per D-10, D-11 (display sample outputs showing parsed content).

**Step 1: Execute notebook**

Commands:
```bash
cd project/
uv run jupyter nbconvert --to notebook --execute --inplace owasp_homework.ipynb
```

This will:
1. Start a fresh Jupyter kernel
2. Execute all cells in order
3. Populate cell outputs with execution results
4. Update the notebook file in place with outputs

Expected outputs to verify in notebook:
- Cell 10: "Downloading OWASP/www-project-top-10-for-large-language-model-applications...", "Total files in archive: N", "Markdown files found: N"
- Cell 11: Repository analysis showing document counts, frontmatter breakdown, 3 sample documents with filenames/metadata/content previews, comparison table

The outputs demonstrate per D-11 (document count and structure differences) and D-12 (verify frontmatter parsing works - returns empty dicts correctly).

**Step 2: Verify pre-commit hooks pass**

Per CLAUDE.md §Linting and Formatting: "All code must pass linting and formatting checks before a commit is accepted. This is enforced via pre-commit hooks — no exceptions, no manual bypasses."

Commands:
```bash
cd project/
uv run pre-commit run --all-files
```

This runs all configured hooks (black, ruff, file hygiene) on the notebook. If any hooks fail:
- Black will auto-format the notebook JSON
- Ruff will auto-fix issues where possible
- Re-run until all hooks pass

Expected output: All hooks should pass (green checkmarks) or auto-fix and pass on second run.

**Step 3: Commit executed notebook**

After successful execution and pre-commit verification, commit the notebook with outputs using GPG-signed conventional commit per CLAUDE.md §Commit Hygiene.

Commands:
```bash
cd project/
git add owasp_homework.ipynb
git commit -S -m "feat(05-project-homework): add OWASP homework notebook with outputs"
```

The commit:
- Uses conventional commit format (feat type, phase scope)
- Is GPG-signed (-S flag)
- Includes the executed notebook with populated outputs
- Has passed all pre-commit hooks automatically
  </action>
  <acceptance_criteria>
    - `cd project/ && uv run jupyter nbconvert --to notebook --execute --inplace owasp_homework.ipynb` exits with status 0
    - `grep -q "Total files in archive" project/owasp_homework.ipynb` succeeds (output populated)
    - `grep -q "Successfully processed" project/owasp_homework.ipynb` succeeds (output populated)
    - `grep -q "Sample documents" project/owasp_homework.ipynb` succeeds (output populated)
    - `cd project/ && uv run pre-commit run --all-files` exits with status 0 (all hooks pass)
    - `git log -1 --format=%B` contains "feat(05-project-homework)" or similar conventional commit prefix
    - `git log -1 --format=%G?` returns "G" (GPG-signed commit)
  </acceptance_criteria>
  <verify>
    <automated>cd project/ && uv run jupyter nbconvert --to notebook --execute --inplace owasp_homework.ipynb && uv run pre-commit run --all-files</automated>
  </verify>
  <done>
Notebook executed successfully from fresh kernel. Outputs show OWASP repository downloaded (542 files), parsed correctly (all with empty metadata as expected), and sample documents displayed with graceful no-frontmatter handling. Pre-commit hooks verified and passed (black formatting, ruff linting, file hygiene). Executed notebook committed to repository with GPG-signed conventional commit message.
  </done>
</task>

</tasks>

<verification>
**Phase-level checks:**

1. **Project structure verification:**
   ```bash
   ls -la project/
   # Should show: pyproject.toml, .python-version, .pre-commit-config.yaml, owasp_homework.ipynb, .venv/, .gitignore
   ```

2. **Dependency verification:**
   ```bash
   cd project/ && uv pip list | grep -E "(requests|frontmatter|jupyter|pre-commit|ruff|black)"
   # Should show all packages installed
   ```

3. **Pre-commit hooks verification:**
   ```bash
   test -f project/.git/hooks/pre-commit && echo "Hooks installed"
   # Should confirm hooks are installed
   ```

4. **Notebook execution verification:**
   ```bash
   cd project/ && uv run jupyter nbconvert --to notebook --execute owasp_homework.ipynb --output /tmp/test_output.ipynb
   # Should exit 0, indicating notebook runs without errors
   ```

5. **Pre-commit verification:**
   ```bash
   cd project/ && uv run pre-commit run --all-files
   # Should exit 0, all hooks pass
   ```

6. **Output verification:**
   ```bash
   grep -A 5 "Total documents:" project/owasp_homework.ipynb
   # Should show actual document count and frontmatter breakdown in outputs
   ```

7. **Engineering standards verification:**
   ```bash
   grep -c "def.*->.*:" project/owasp_homework.ipynb
   # Should return >0 (type hints present on functions)

   grep -c "Args:" project/owasp_homework.ipynb
   # Should return >0 (Google-style docstrings present)
   ```

8. **Behavioral verification (must_haves):**
   - Document count shown for OWASP repository (542 expected per research)
   - All documents show empty metadata (no frontmatter in OWASP)
   - Sample outputs display 3 documents with filename, metadata status, content preview
   - Comparison table shows OWASP vs course repos differences
   - Educational reflection explains what was learned

All verifications should pass before marking phase complete.
</verification>

<success_criteria>
**Phase complete when:**

1. ✓ project/ folder contains valid uv project with pyproject.toml and .python-version
2. ✓ Dependencies (requests, python-frontmatter, jupyter, pre-commit, ruff, black) installed and verified
3. ✓ Pre-commit hooks configured and installed (.pre-commit-config.yaml, .git/hooks/pre-commit exist)
4. ✓ Notebook exists with complete implementation (imports, helpers, main function, tests)
5. ✓ Notebook successfully downloads OWASP LLM Top 10 repository (542 markdown files)
6. ✓ Parser extracts all markdown documentation with correct structure (filename, metadata, content)
7. ✓ Verification shows data extraction works despite OWASP having no frontmatter (all metadata dicts empty)
8. ✓ Sample outputs display showing parsed content from OWASP repository
9. ✓ Educational cells document differences between OWASP and course repos
10. ✓ Pre-commit hooks pass before commit (black, ruff, file hygiene all green)
11. ✓ Notebook executes cleanly from fresh kernel with all outputs committed
12. ✓ Engineering standards applied (type hints, docstrings per D-03, pre-commit enforcement per CLAUDE.md)

**Measurable outcomes:**
- `project/pyproject.toml` exists and declares all dependencies
- `project/.pre-commit-config.yaml` exists with ruff and black hooks
- `project/.git/hooks/pre-commit` exists (hooks installed)
- `project/owasp_homework.ipynb` exists with 12+ cells
- Notebook execution completes without errors: `uv run jupyter nbconvert --execute --inplace owasp_homework.ipynb` exits 0
- Pre-commit verification passes: `uv run pre-commit run --all-files` exits 0
- Notebook outputs show "Successfully processed 542 documents" or similar count
- All documents in output show `Metadata: None (no frontmatter)` or empty `{}`
- Reflection cell explains OWASP differences and frontmatter handling insights
</success_criteria>

<output>
After completion, create `.planning/phases/05-project-homework/05-01-SUMMARY.md` documenting:
- What was implemented (uv project, pre-commit hooks, notebook, OWASP testing)
- How implementation adapted course patterns to different repo structure
- Key insights about frontmatter-optional markdown parsing
- Differences observed between OWASP (542 files, no frontmatter) and course repos
- Engineering standards applied (type hints, docstrings, pre-commit hooks, GPG commits)
</output>
